# Default values for helm-chart-publisher.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: jbrownsc/helm-chart-publisher
  tag: release
  pullPolicy: Always
service:
  name: helm-chart-publisher
  type: ClusterIP
  externalPort: 80
  internalPort: 8080
ingress:
  enabled: false
  # Used to create an Ingress record.
  hosts:
    - helm.your.domain
  annotations:
    #kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  tls:
    # Secrets must be manually created in the namespace.
    # - secretName: chart-example-tls
    #   hosts:
    #     - chart-example.local
resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
  #  memory: 128Mi
helm_chart_publisher:
  config.yaml: >
    repos:
      - name: internal
        bucket: your-bucket-name
    storage:
      # Only one can be enabled
      s3:
        region: us-west-2
        bucket: your-bucket-name
      # gcs: {} # uses GCloud Application Default Credentials
      # swift:
      #   username: SWIFT_USERNAME
      #   password: SWIFT_USERNAME
      #   tenant: some_tenant
      #   authUrl: https://some-auth-url:5000/v2.0
      #   endpointType: admin
      #   container: "kube-charts"
      #   insecureSkipVerify: false
